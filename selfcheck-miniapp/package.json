{
  "name": "selfcheck-miniapp",
  "private": true,
  "version": "0.1.0",
  "description": "Telegram Mini App for personal data-exposure SELF-checks. Results are released only after ownership verification (email code / phone OTP), so it cannot be used to look up other people.",
  "workspaces": [
    "server",
    "web"
  ],
  "scripts": {
    "dev:server": "npm run dev --workspace server",
    "dev:web": "npm run dev --workspace web",
    "build": "npm run build --workspace web && npm run build --workspace server",
    "start": "npm run start --workspace server",
    "test": "npm run test --workspace server",
    "typecheck": "npm run typecheck --workspace server && npm run typecheck --workspace web"
  },
  "engines": {
    "node": ">=20"
  }
}
